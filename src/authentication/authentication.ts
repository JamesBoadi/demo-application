import React from 'react';

const authentication = () => {

  // Call to server to load page details from database
  function authenticateUser (){
  }

  // Logout user
  const logoutUser = () => {
  }

  // Login user
  const loginUser= () => {

    // Authenticates user using oath2 or some other library
    authenticateUser (); 
  }

}

export default authentication;
