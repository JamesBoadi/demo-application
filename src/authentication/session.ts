import React from 'react';

const session = () => {

  // Checks if session has expired
  const sessionExpired = () => {
  }

  // Creates new session if the user has successfully logged in
  const createNewSession = () => {
  }

}

export default session;